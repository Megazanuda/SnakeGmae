<Window x:Class="WpfApp6.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp6"
        mc:Ignorable="d"
        Title="Старт" Height="250" Width="400" ResizeMode="NoResize">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Canvas Name="PaintCanvas" MaxWidth="800" MaxHeight="500" Grid.ColumnSpan="2" Margin="5,0,5,0" />


        <TextBox x:Name="Name" Text ="" FontSize="25" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Column="1" Width="175" Height="67" InputScope="LogOnName" AutomationProperties.Name="" AutomationProperties.HelpText="Введите ник" KeyDown="baza" Margin="106,100,113,54"/>
        <Label Grid.Column="1" Content="               Игра Змейка. &#xD;&#xA;   Введите ваш ник для старта." HorizontalAlignment="Center" Margin="0,23,0,0" VerticalAlignment="Top" FontSize="16" HorizontalContentAlignment="Center"/>
    </Grid>
</Window>